[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short

# Coverage settings (when pytest-cov is installed)
# Run with: pytest --cov=.. --cov-report=term-missing
# Example: pytest --cov=.. --cov-report=term-missing --cov-report=html
